import { IMTitleSampleDirective } from './imtitle-sample.directive';

describe('IMTitleSampleDirective', () => {
  it('should create an instance', () => {
    const directive = new IMTitleSampleDirective();
    expect(directive).toBeTruthy();
  });
});
