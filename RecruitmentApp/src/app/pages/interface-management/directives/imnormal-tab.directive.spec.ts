import { IMNormalTabDirective } from './imnormal-tab.directive';

describe('IMNormalTabDirective', () => {
  it('should create an instance', () => {
    const directive = new IMNormalTabDirective();
    expect(directive).toBeTruthy();
  });
});
