import { IMStepperDirective } from './imstepper.directive';

describe('IMStepperDirective', () => {
  it('should create an instance', () => {
    const directive = new IMStepperDirective();
    expect(directive).toBeTruthy();
  });
});
