import { IMHoverTabDirective } from './imhover-tab.directive';

describe('IMHoverTabDirective', () => {
  it('should create an instance', () => {
    const directive = new IMHoverTabDirective();
    expect(directive).toBeTruthy();
  });
});
