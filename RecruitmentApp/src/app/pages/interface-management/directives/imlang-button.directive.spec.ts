import { IMLangButtonDirective } from './imlang-button.directive';

describe('IMLangButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new IMLangButtonDirective();
    expect(directive).toBeTruthy();
  });
});
