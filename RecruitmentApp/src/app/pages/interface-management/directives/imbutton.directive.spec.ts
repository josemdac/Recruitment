import { IMButtonDirective } from './imbutton.directive';

describe('IMButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new IMButtonDirective();
    expect(directive).toBeTruthy();
  });
});
