import { ImMainSiteImageDirective } from './im-main-site-image.directive';

describe('ImMainSiteImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ImMainSiteImageDirective();
    expect(directive).toBeTruthy();
  });
});
