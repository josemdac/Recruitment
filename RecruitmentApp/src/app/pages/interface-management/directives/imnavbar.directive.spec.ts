import { IMNavbarDirective } from './imnavbar.directive';

describe('IMNavbarDirective', () => {
  it('should create an instance', () => {
    const directive = new IMNavbarDirective();
    expect(directive).toBeTruthy();
  });
});
