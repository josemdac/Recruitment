import { IMImageDirective } from './imimage.directive';

describe('IMImageDirective', () => {
  it('should create an instance', () => {
    const directive = new IMImageDirective();
    expect(directive).toBeTruthy();
  });
});
