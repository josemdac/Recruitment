import { IMFooterSmallDirective } from './imfooter-small.directive';

describe('IMFooterSmallDirective', () => {
  it('should create an instance', () => {
    const directive = new IMFooterSmallDirective();
    expect(directive).toBeTruthy();
  });
});
