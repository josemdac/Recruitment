import { IMContentAreaDirective } from './imcontent-area.directive';

describe('IMContentAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new IMContentAreaDirective();
    expect(directive).toBeTruthy();
  });
});
