import { IMHeaderDirective } from './imheader.directive';

describe('IMHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new IMHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
