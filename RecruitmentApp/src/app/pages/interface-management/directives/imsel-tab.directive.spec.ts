import { IMSelTabDirective } from './imsel-tab.directive';

describe('IMSelTabDirective', () => {
  it('should create an instance', () => {
    const directive = new IMSelTabDirective();
    expect(directive).toBeTruthy();
  });
});
