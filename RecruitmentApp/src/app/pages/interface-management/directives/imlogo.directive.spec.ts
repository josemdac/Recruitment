import { IMLogoDirective } from './imlogo.directive';

describe('IMLogoDirective', () => {
  it('should create an instance', () => {
    const directive = new IMLogoDirective();
    expect(directive).toBeTruthy();
  });
});
