import { UserDocEditGridDirective } from './user-doc-edit-grid.directive';

describe('UserDocEditGridDirective', () => {
  it('should create an instance', () => {
    const directive = new UserDocEditGridDirective();
    expect(directive).toBeTruthy();
  });
});
