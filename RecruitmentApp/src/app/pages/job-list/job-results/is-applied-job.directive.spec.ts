import { IsAppliedJobDirective } from './is-applied-job.directive';

describe('IsAppliedJobDirective', () => {
  it('should create an instance', () => {
    const directive = new IsAppliedJobDirective();
    expect(directive).toBeTruthy();
  });
});
