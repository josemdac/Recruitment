import { JobTvDirective } from './job-tv.directive';

describe('JobTvDirective', () => {
  it('should create an instance', () => {
    const directive = new JobTvDirective();
    expect(directive).toBeTruthy();
  });
});
