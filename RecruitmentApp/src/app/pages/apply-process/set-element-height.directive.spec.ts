import { SetElementHeightDirective } from './set-element-height.directive';

describe('SetElementHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new SetElementHeightDirective();
    expect(directive).toBeTruthy();
  });
});
