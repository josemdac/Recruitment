import { StepperHeightDirective } from './stepper-height.directive';

describe('StepperHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new StepperHeightDirective();
    expect(directive).toBeTruthy();
  });
});
