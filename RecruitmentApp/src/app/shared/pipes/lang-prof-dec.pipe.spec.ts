import { LangProfDecPipe } from './lang-prof-dec.pipe';

describe('LangProfDecPipe', () => {
  it('create an instance', () => {
    const pipe = new LangProfDecPipe();
    expect(pipe).toBeTruthy();
  });
});
