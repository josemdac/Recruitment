import { StatusDecPipe } from './status-dec.pipe';

describe('StatusDecPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusDecPipe();
    expect(pipe).toBeTruthy();
  });
});
