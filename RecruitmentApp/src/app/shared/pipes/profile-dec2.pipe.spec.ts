import { ProfileDec2Pipe } from './profile-dec2.pipe';

describe('ProfileDec2Pipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileDec2Pipe();
    expect(pipe).toBeTruthy();
  });
});
