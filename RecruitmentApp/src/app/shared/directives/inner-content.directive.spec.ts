import { InnerContentDirective } from './inner-content.directive';

describe('InnerContentDirective', () => {
  it('should create an instance', () => {
    const directive = new InnerContentDirective();
    expect(directive).toBeTruthy();
  });
});
