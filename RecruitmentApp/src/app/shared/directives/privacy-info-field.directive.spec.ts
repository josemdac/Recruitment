import { PrivacyInfoFieldDirective } from './privacy-info-field.directive';

describe('PrivacyInfoFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new PrivacyInfoFieldDirective();
    expect(directive).toBeTruthy();
  });
});
