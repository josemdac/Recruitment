import { ApplicStatDecDirective } from './applic-stat-dec.directive';

describe('ApplicStatDecDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplicStatDecDirective();
    expect(directive).toBeTruthy();
  });
});
