import { ShareBtnCustomDirective } from './share-btn-custom.directive';

describe('ShareBtnCustomDirective', () => {
  it('should create an instance', () => {
    const directive = new ShareBtnCustomDirective();
    expect(directive).toBeTruthy();
  });
});
