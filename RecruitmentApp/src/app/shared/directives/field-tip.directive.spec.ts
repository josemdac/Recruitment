import { FieldTipDirective } from './field-tip.directive';

describe('FieldTipDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldTipDirective();
    expect(directive).toBeTruthy();
  });
});
