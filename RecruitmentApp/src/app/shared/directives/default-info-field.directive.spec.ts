import { DefaultInfoFieldDirective } from './default-info-field.directive';

describe('DefaultInfoFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultInfoFieldDirective();
    expect(directive).toBeTruthy();
  });
});
