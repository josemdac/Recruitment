import { ProfileTypeCellDirective } from './profile-type-cell.directive';

describe('ProfileTypeCellDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfileTypeCellDirective();
    expect(directive).toBeTruthy();
  });
});
