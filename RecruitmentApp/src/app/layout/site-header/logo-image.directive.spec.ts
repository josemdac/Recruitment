import { LogoImageDirective } from './logo-image.directive';

describe('LogoImageDirective', () => {
  it('should create an instance', () => {
    const directive = new LogoImageDirective();
    expect(directive).toBeTruthy();
  });
});
