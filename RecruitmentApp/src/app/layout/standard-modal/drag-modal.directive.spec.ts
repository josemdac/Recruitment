import { DragModalDirective } from './drag-modal.directive';

describe('DragModalDirective', () => {
  it('should create an instance', () => {
    const directive = new DragModalDirective();
    expect(directive).toBeTruthy();
  });
});
