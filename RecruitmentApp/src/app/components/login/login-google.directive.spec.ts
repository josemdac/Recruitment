import { LoginGoogleDirective } from './login-google.directive';

describe('LoginGoogleDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginGoogleDirective();
    expect(directive).toBeTruthy();
  });
});
