import { LoginTwitterDirective } from './login-twitter.directive';

describe('LoginTwitterDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginTwitterDirective();
    expect(directive).toBeTruthy();
  });
});
