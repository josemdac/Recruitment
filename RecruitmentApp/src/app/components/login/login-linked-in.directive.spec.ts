import { LoginLinkedInDirective } from './login-linked-in.directive';

describe('LoginLinkedInDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginLinkedInDirective();
    expect(directive).toBeTruthy();
  });
});
