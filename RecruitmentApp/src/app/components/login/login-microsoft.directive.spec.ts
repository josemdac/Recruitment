import { LoginMicrosoftDirective } from './login-microsoft.directive';

describe('LoginMicrosoftDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginMicrosoftDirective();
    expect(directive).toBeTruthy();
  });
});
