import { LoginFacebookDirective } from './login-facebook.directive';

describe('LoginFacebookDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginFacebookDirective();
    expect(directive).toBeTruthy();
  });
});
