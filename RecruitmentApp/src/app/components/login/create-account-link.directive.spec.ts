import { CreateAccountLinkDirective } from './create-account-link.directive';

describe('CreateAccountLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new CreateAccountLinkDirective();
    expect(directive).toBeTruthy();
  });
});
