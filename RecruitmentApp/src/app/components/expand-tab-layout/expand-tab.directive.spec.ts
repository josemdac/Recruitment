import { ExpandTabDirective } from './expand-tab.directive';

describe('ExpandTabDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandTabDirective();
    expect(directive).toBeTruthy();
  });
});
