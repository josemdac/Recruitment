import { ExpandTabButtonDirective } from './expand-tab-button.directive';

describe('ExpandTabButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandTabButtonDirective();
    expect(directive).toBeTruthy();
  });
});
