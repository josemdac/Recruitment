import { FormInputDateDirective } from './form-input-date.directive';

describe('FormInputDateDirective', () => {
  it('should create an instance', () => {
    const directive = new FormInputDateDirective();
    expect(directive).toBeTruthy();
  });
});
