import { FormInputTelDirective } from './form-input-tel.directive';

describe('FormInputTelDirective', () => {
  it('should create an instance', () => {
    const directive = new FormInputTelDirective();
    expect(directive).toBeTruthy();
  });
});
