import { FormInputPasswordDirective } from './form-input-password.directive';

describe('FormInputPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new FormInputPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
