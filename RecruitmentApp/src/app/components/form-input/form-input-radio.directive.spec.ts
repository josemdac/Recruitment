import { FormInputRadioDirective } from './form-input-radio.directive';

describe('FormInputRadioDirective', () => {
  it('should create an instance', () => {
    const directive = new FormInputRadioDirective();
    expect(directive).toBeTruthy();
  });
});
