import { FormInputSelectSourceDirective } from './form-input-select-source.directive';

describe('FormInputSelectSourceDirective', () => {
  it('should create an instance', () => {
    const directive = new FormInputSelectSourceDirective();
    expect(directive).toBeTruthy();
  });
});
