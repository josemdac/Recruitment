import { FormInputMultiselectDirective } from './form-input-multiselect.directive';

describe('FormInputMultiselectDirective', () => {
  it('should create an instance', () => {
    const directive = new FormInputMultiselectDirective();
    expect(directive).toBeTruthy();
  });
});
