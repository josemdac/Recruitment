import { FormInputFileDirective } from './form-input-file.directive';

describe('FormInputFileDirective', () => {
  it('should create an instance', () => {
    const directive = new FormInputFileDirective();
    expect(directive).toBeTruthy();
  });
});
